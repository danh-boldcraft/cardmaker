# Local Development Environment Configuration
# Copy this to .env.local and modify as needed

# Backend Server Configuration
PORT=3001
HOST=localhost

# Frontend Configuration
FRONTEND_PORT=8080

# API Configuration
# Set to 'local' to use local backend, 'deployed' to use AWS backend
API_ENV=local

# AWS API Endpoint (for deployed backend)
AWS_API_ENDPOINT=https://usyl5zha62.execute-api.us-west-2.amazonaws.com/prod/multiply

# Local Backend Endpoint
LOCAL_API_ENDPOINT=http://localhost:3001/multiply

# Debug Mode (log API requests and responses)
DEBUG=true
